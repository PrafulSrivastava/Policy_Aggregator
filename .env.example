# Database Configuration
# PostgreSQL connection string
# Format: postgresql://user:password@host:port/database
# For local development: postgresql://postgres:postgres@localhost:5432/policy_aggregator
DATABASE_URL=postgresql://user:password@localhost:5432/policy_aggregator

# JWT Authentication Configuration
# Secret key for JWT token generation (use a strong random string in production)
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=your-secret-key-here-change-in-production

# JWT algorithm (HS256 is recommended for most use cases)
JWT_ALGORITHM=HS256

# JWT token expiration time in hours
JWT_EXPIRE_HOURS=24

# Email Service Configuration (Resend)
# API key for Resend email service
# Get your API key from: https://resend.com/api-keys
# Optional for development, required for production email alerts
RESEND_API_KEY=re_your_api_key_here

# Application Environment
# Options: development, staging, production
ENVIRONMENT=development

# Logging Configuration
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
